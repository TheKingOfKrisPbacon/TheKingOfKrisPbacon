def Fibonacci(n):
  currentNumber = 1
  arr = [1,1]

  if n > 2:
    for number in range(2, n):
      currentNumber = arr[-1] + arr[-2]
      arr.append(currentNumber)
  
  return currentNumber

n = input("Please enter a positive number" + '\n')
result = Fibonacci(int(n))
print(result)
